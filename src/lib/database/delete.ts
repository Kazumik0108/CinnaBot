// import { Connection } from 'typeorm';
// import { Channel } from '../../entity';

// export async function deleteChannel(conn: Connection, ids: string[]) {
//   for (const id of ids) {
//     await conn.createQueryBuilder().delete().from(Channel).where('id = :id', { id: id }).execute();
//   }
// }
